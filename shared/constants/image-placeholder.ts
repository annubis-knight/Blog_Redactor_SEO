/**
 * Emplacement d'image à fournir (FR-RED-ENRICH-PASSES, passe « images »).
 *
 * La passe images propose OÙ placer une image et CE qu'elle doit montrer (texte
 * alternatif) ; la photo elle-même reste à fournir. Cette image neutre tient la
 * place dans l'éditeur, et la porte de publication refuse de publier tant qu'elle
 * n'a pas été remplacée.
 */
export const IMAGE_TO_PROVIDE_SRC = '/images/image-a-fournir.svg'
